using System;

namespace Start
{
    class Test
    {
        public static void Main()
        {
            int step = 7;
            int endNumbers = 100;
            
            for (int number = step; number < endNumbers; number += step)
            {
                Console.WriteLine(number);
            }
        }
    }
}
